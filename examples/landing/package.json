{
	"name": "@worken/examples-landing",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"build-bundle": "bun run ./dist/build-bundle.js",
		"lint": "biome lint .",
		"check-types": "tsc -p tsconfig.json --noEmit",
		"clean": "rm -rf dist .turbo"
	},
	"dependencies": {
		"@worken/context-core": "workspace:*",
		"@worken/platform-core": "workspace:*",
		"@worken/semantic-core": "workspace:*",
		"@worken/semantic-ir": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"@worken/tsconfig": "workspace:*",
		"typescript": "^5.9.3"
	}
}
