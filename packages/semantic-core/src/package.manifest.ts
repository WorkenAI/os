/**
 * Colocated manifest metadata for @worken/semantic-core (authoring / tooling).
 */
export const packageManifest = {
	name: "@worken/semantic-core",
	role: "operational-semantics",
	protocol: "semantic",
} as const;
